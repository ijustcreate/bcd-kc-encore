Original prompt: I’m not liking what we have. For now make me my own copy of this Celestefall on GitHub And get it running in place of what we have.

## 2026-09-05 — Truthful room-full and disconnect state

- Rejection responses now include the authoritative room occupancy when available. The browser preserves `null` for unknown occupancy after disconnect/rejection, clears stale remotes/effects, and only renders `ROOM FULL · N/8` for a verified bounded count; legacy responses render `ROOM FULL` without inventing `8/8`.
- Authority network tests and the headless desktop/mobile browser test pass, including reconnect cleanup, exact full-room occupancy, and no client-side shared simulation.

## 2026-09-05 — Coordinated 1.1 authoritative gameplay integration

- Replaced peer Supabase gameplay broadcasts with a dedicated Node/WebSocket authority. Server simulates all players, AI, projectiles, damage/death/respawn, moving platforms, captures and pickups at 60Hz; clients send controls at30Hz and render complete15Hz snapshots. Shared simulation remains disabled in clients during disconnect/rejection. Server assigns IDs/resume tokens, admits8 without evicting existing members, expires stale inputs and clears disconnected/rejected UI/remotes.
- Integrated scoped respawn, corpse, heart, capture marker/fill, account-name and health-dot tasks. Hearts roll once on server lethal transitions, heal one actual health point for one winner, and disappear in every snapshot. Helpers also support clearly labeled single-player practice. Preserved v1.0 reload/asset freshness/shutdown and6b0bfe6 idle-head correction; separate WebGL2 checkout untouched.
- Verification:9 authority network/respawn tests and9 heart/corpse tests pass; real headless desktop/mobile browser test passes shared bot/slug/hearts, keyboard combat, late join, single pickup, reconnect, eight-slot rejection/cleanup and inability of browser time to advance shared simulation. Account-name, capture marker, bat corpse and idle-head regressions pass; standard web-game client smoke and screenshots inspected. An account-name test timed out during concurrent browser runs, then passed alone; bat test initially omitted its required admin=1 URL, corrected run passed.
- Eight-player sample:6,451-byte JSON snapshot compressed to916 bytes.10,000 local server ticks measured162.6ms in one smoke sample (not production/physical-phone certification). No extra client AI simulation added. Original physical-phone1FPS issue is not certified resolved.
- Public hosting remains a deployment requirement: verified existing Supabase project has only profile/achievement/chat Edge Functions; GitHub Pages cannot run this persistent process. No paid resources created. Added server/README.md and Dockerfile. Empty authority-config.js explicitly selects OFFLINE PRACTICE, never falsely LIVE. New bcd-kc-encore repository created for canonical URL, preserving old Celestefall link; release/public verification still pending below.
- Phone diagnostics release completed on the canonical game: opt-in sampling reports measured rAF/presented FPS, frame intervals/stalls, update/render cost, viewport/screen/DPR/orientation/browser state, renderer/quality/throttle, asset failures, and canonical room admission without exposing storage or telemetry. Public phone-sized headless flow confirmed OFFLINE PRACTICE and unknown occupancy, preserving v1.2 capture-fill behavior. Physical iPhone performance remains unverified until a user shares the panel screenshot.

## 2026-09-03 — Encore Royale combat and color-team pass

- Replaced the bat's overly narrow dive collision with a matching radius-based impact check, so a visible dive now reliably lands one hit.
- Player knockouts now hold the fallen location for a visible, exact three-second `RETURNING 3…2…1` countdown; respawn arrives with an invulnerability window, colored expanding-ring spotlight effect, sparks, and haptics.
- Capture points now use roster color as the team identity: same-color players cannot damage each other, capture together faster when standing in the zone, and share an already-owned point. A different color (including after changing your own color) can fully retake a point.
- Captured zones retain their capturing roster, display the team color and short contributor names in-world, and replace the full roster only when another color completes a takeover.

- Forked `bennyfrancis/Celestefall` to `ijustcreate/Celestefall`, preserving the original GameMaker project and MIT license.
- Added a dependency-free browser adaptation under `docs/` with the original demo art, pixel-step movement, jumping, wall cling/jump, one-way drop-through, moving solids, rider carrying, camera follow, particles, pause/reset/fullscreen, keyboard controls, and responsive touch controls.
- Added BCD iframe ready/init/close messaging plus `render_game_to_text` and `advanceTime` testing hooks.
- Automated canvas smoke test passed for movement, jumping, landing, moving platforms, camera/state output, and browser console errors.
- Portrait 390×844 visual QA passed for the zoomed/cropped arena, joystick, Jump/Grab buttons, safe-area spacing, embedded close-button clearance, and zero console warnings/errors.
- TODO: tune the web port against direct side-by-side GameMaker runtime footage if a licensed HTML5 export becomes available; current port is source-guided, not an exported GameMaker binary.
- Three-screen movement pass: confirmed upstream has only the Treasure Hunters pirate state set, then expanded the web controller with eased acceleration/deceleration, lower top speed, coyote time, jump buffering, variable jump height, crouch, look-up, and gentler wall cling/jump. Replaced the small demo room with a 1920×360 level (exactly one display tall and three 640px camera widths wide), three visual path sections, static and moving one-way platforms, a wall-cling obstacle, and a horizontally following look-ahead camera.
- Three-screen QA: the automated route traversed from x=120 to x=1897 with zero deaths while the camera reached its exact right clamp at x=1280. Held-input portrait checks confirmed both `crouch` and `look` states, their visible poses, centered camera overscan at the left edge, and zero browser warnings/errors. The initial cling wall was lowered after the first route review so ordinary jumping remains a viable path.
- TODO: replace the pirate with a bespoke original archer sprite sheet once a character direction is chosen; the upstream repository contains no alternate TowerFall-like character art.
- Bullet Age Ash pass: located the newest `BulletAge-WorkingCopy` project and imported Ash's original Spine 3.7.87 skeleton, atlas, and texture into the web build without Unity. Added the matching official Spine 3.7.94 canvas runtime, an isolated Ash adapter, authored-state mapping for idle/run/jump/fall/wall-cling/crouch/look-up, larger Ash-sized collision bounds, and a pirate load-failure fallback. Ash's proprietary asset status and the Spine runtime license are documented alongside the files.
- Ash QA: verified her assembled rig and Idle state in the standard harness; visually inspected portrait Crouch_Idle and aim-up poses at 390×844; confirmed the text state matches the rendered animations; found zero browser warnings/errors; and traversed the full 1920px route to x=1895 in section 3 with the camera at its x=1280 clamp and zero deaths.
- Combat/bot pass: removed the manual grab action and made wall cling automatic whenever Ash is airborne and pressing toward a wall; the same Jump control now launches a wall jump. Replaced Grab with musical-note Shoot, added directional Dash, projectile trails, hit pushback, vibration hooks, and one blue Player2 Bullet Age bot with three health pips and infinite respawns. The bot uses its original Spine 3.7 animations and now avoids point-blank firing so it cannot pin Ash at an edge.
- Combat QA: verified Shoot lowers the bot from 3 to 2 health and pushes it back; repeated shots produced a knockout and respawn; verified the directional Dash enters Ash's `Dash_Pistol` state; verified the complete jump-to-wall -> automatic `Wall_Cling` -> wall-jump-away chain; traversed to x=1791 in section 3 with zero deaths; and found zero browser errors or warnings. Visually inspected the programmatic notes/bot in the standard game harness, the final 390×844 portrait controls, and a coarse-pointer 844×390 landscape layout. The landscape canvas now preserves 16:9 instead of horizontally stretching during rotation.
- Ash Skin Studio: added an admin-only top-right workshop opened by the BCD session handshake. It uses a second live Spine preview rig, exposes all included equipment attachment skins, separately tints head/hood, body/arms, pack/gear, and weapon slot groups, previews idle/run/jump/fall/cling/crouch/look/shoot/dash, and supports named create/update/delete with device-local persistence. The selected custom skin is restored onto the playable Ash rig for every user on that device, while only admins can open the editor.
- Skin Studio QA: the standard game harness passed with `skinEditor.admin=true` and no errors; portrait browser QA created a four-layer cyan/purple/gold/pink `Midnight Test` skin using `sniper_01`, previewed Dash, saved it, reloaded it with all values intact, visually confirmed it on playable Ash, and then verified the editor button and dialog are absent without admin authorization.
- Live combat input pass: Shoot is now a hold-to-aim/release-to-fire action with a full analog direction vector and in-world aim guide; projectiles travel and trail at arbitrary angles. Added Ash's authored Sword_Attack_01 as a dedicated melee action, plus one-damage enemy head stomps with a bounce. Removed every player-facing pause/reset path, including the skin-editor pause and host-close pause.
- Landscape presentation pass: coarse-pointer landscape now uses a full-viewport cover canvas rather than reserving a controller tray. The controls are a transparent overlay with only the softened joystick and four action buttons visible.
- Live combat QA: the standard deterministic harness passed movement/jump/render-state with both Spine rigs and no errors. Browser checks verified diagonal aim at `(0.71, -0.71)`, release velocity `(4.38, -4.38)`, Sword_Attack_01 reducing bot health by one, and an actual head stomp reducing health by one while bouncing Ash at `vy=-6.7`. The former P key leaves mode playing. Visually inspected 844×390 coarse-pointer full-bleed landscape and 390×844 portrait layouts; console reported zero errors and warnings.
- TODO: the no-pause client now behaves like an always-active arena while open, but this repository still has local bot simulation only. True shared players, authoritative room state, and room lifetime based on connected-player count require a realtime backend in a later multiplayer phase.
- Control-layout correction: bottom-anchored the cover-scaled landscape canvas so panoramic cropping removes sky instead of the playable floor. Landscape actions are now a clean horizontal row; portrait phones through 520px use a compact, non-overlapping diamond that keeps Jump inside the viewport.
- Embedded HUD correction: the game now detects a real iframe in addition to the `embed=1` query and reserves 58px for BCD's parent-level Close button, keeping admin Skin Studio and Help controls out from underneath it.
- Control-layout QA: deterministic movement/state smoke tests remained clean. Visually verified a coarse-pointer 844×390 landscape with the entire ground strip visible and all four actions separated in one row, plus 430×932 portrait with the joystick and all four actions fully inside the viewport. Embedded admin preview keeps Skin Studio and Help left of the reserved parent Close region. Browser console: zero errors and warnings.
- Directional sword pass: added Ash's authored Sword_Attack_Up and Sword_Attack_Down states. Holding the stick/keyboard up or down while pressing Melee now selects the matching animation, vertical hitbox, and directional knockback; neutral Melee retains Sword_Attack_01 and its forward hitbox.
- Player2 directional sword pass: confirmed the P2 Spine rig contains the same exact up/down sword clips and added close-range bot melee selection based on Ash's relative height. P2 now uses forward, up, or down hitboxes/knockback and the matching authored animation rather than remaining ranged-only.
- Directional sword QA: asset inspection confirmed both Ash and Player2 contain `Sword_Attack_Up` and `Sword_Attack_Down`. Deterministic browser input verified Up+Melee resolves to `meleeUp` / `Sword_Attack_Up` and Down+Melee resolves to `meleeDown` / `Sword_Attack_Down`; standard movement/render smoke remained clean and the browser console reported zero errors or warnings.
- Sword-clarity fix: melee timing now follows each authored clip, keeping Sword_Attack_Up active through its delayed 0.17–0.33s slash effect and aligning directional hit windows with the visible blade. The Spine adapter forcibly clears the unkeyed Gun slot during every sword state so pistol crossfades cannot cover a slash. Player2 anti-air decisions are now gated by a 72–101 frame cooldown and only counter 28% of descending stomp approaches (42% while the player is still rising), rather than retrying an upward slash every frame.
- Sword-clarity QA: the standard deterministic harness stayed error-free; an exact held-Up + Melee browser chord reported `meleeUp` / `Sword_Attack_Up` throughout the visual effect window, and the inspected portrait frame visibly contains the full white sword arc with no pistol. A live jump-over-bot route produced no upward counter, confirming overhead approaches no longer guarantee `meleeUp`; browser console remained at zero errors and warnings.
- Bullet Age creature pass: imported the original Spine 3.6.53 Bat and Slug rigs from the latest local `BulletAge-WorkingCopy` without Unity. The arena now maintains two independent bats and two independent slugs: bats hover/chase/dive and die in one hit, slugs patrol/chase/bite and have two health, and every creature uses its authored idle/move/attack/hit/death animations before respawning at its own home position after two seconds.
- Bullet Age creature QA: the standard harness reported exactly two live bats and two live slugs with all four original rigs loaded (`fly` / `walk`) and visual inspection confirmed the first bat and slug are readable and separated from Player2. Real Shoot input reduced the slug from two health to one, killed it with the second shot, and independently killed a bat with one shot; both reported `death`, stayed dead mid-timer, and respawned at full health with `walk` / `fly` after two seconds. Proximity tests exercised each authored attack and confirmed Ash receives the hit. Console: zero errors and warnings.
- Aim-memory fix: neutral Shoot now derives its direction only from Ash's current facing instead of reusing the previous shot vector, and each fired shot immediately clears its stored vertical aim back to horizontal. This keeps deliberate held-stick aiming intact while making the next centered shot fire straight ahead.
- Aim-memory QA: the standard deterministic game harness stayed clean, then an exact browser regression fired one held-Up shot at `(0, -6.2)`, released every control, and fired the next shot at `(6.2, 0)` with final aim reset to `(1, 0)`. Visual inspection passed and the browser console reported zero errors and warnings.
- Pixel Character Studio implementation: replaced the broken Bullet Age attachment/tint editor with a separate protected 24×32 pixel-fighter system. Ash and Player 2 remain selectable legacy rigs, while custom fighters can mix hair, face, hood, outfit, armor, backpack, and weapon modules; independently color six material/effect groups; preview every movement/combat pose; paint seven constrained visual layers with pencil, eraser, fill, mirror, and undo; and save, duplicate, delete, or select device-local fighters from the in-game carousel. Custom visuals never alter the fixed gameplay hitbox.
- Pixel Character Studio QA: the standard deterministic game harness preserved Ash movement, bot/creature simulation, and all render state with no browser errors. A real 390×844 browser flow built `Morning Hero` with six changed modules, painted and mirrored two protected outfit pixels, saved and selected it, reloaded with the complete design intact, and rendered it as the playable fighter. Duplicate/save/delete changed the local library from one to two and back to one without touching the original. Visually checked the scrollable portrait and 844×390 landscape studios plus portrait gameplay; console reported zero errors and warnings.
- Touch action remap: standardized both portrait and coarse-pointer landscape on the same right-side diamond requested in the marked screenshot: Dash at the top, Melee at the left, Shoot at the bottom, and Jump at the right. CSS placement changes preserve each button's existing ID and gameplay event binding, so the visible action and triggered action remain identical.
- Touch action remap QA: the deterministic game harness kept movement, jump, the selected fighter, bot/creature simulation, and render state error-free. Full-viewport inspection at 390×844 portrait and touch-emulated 844×390 landscape confirmed the identical four-button diamond, readable action labels, no overlap, and the landscape ground strip still visible. Browser console reported zero errors and warnings.
- Gothic performance-tower art pass: generated three original, matched 16:9 pixel-art background panels with the built-in image tool and copied them into the game as `tower-bg-left.png`, `tower-bg-center.png`, and `tower-bg-right.png`. The connected three-screen world now progresses from burgundy backstage tower to ruined opera hall to violet crystal catacomb, with a selective lower-lane contrast veil for fighter readability. Replaced the former pale skyline, green-topped blocks, and plain magenta bars with dark masonry, burgundy inlays, antique-gold edges, and ornate pixel supports.
- Wide arena layout pass: rebuilt collision geometry as three connected TowerFall-like combat chambers across the existing 1920×360 world. Each chamber has mirrored low/mid/crown routes, 42–58px jump steps, low wall-jump plinths, continuous navigable ground, and five moving lifts/carriers; the old full-height interior obstruction was removed.
- Marked touch-position pass: interpreted the green circles as two action rows. Dash and Jump now occupy the upper row; Melee and Shoot occupy the lower row in portrait and touch landscape, while preserving every button's existing action binding.
- Gothic tower QA: the deterministic browser route traversed the rebuilt arena from x=120 to x=1891, reached the exact camera clamp at x=1280, entered section 3, and recorded zero deaths. Lowered both chamber-divider plinths to 48px after traversal review so they remain useful wall-jump anchors without blocking ordinary jumps. Full-viewport visual checks passed at 844×390 landscape and 390×844 portrait: the floor remains visible, all four controls match the marked two-row layout, fighters stay legible against the selective lane veil, and the browser console reported zero errors or warnings.
- Encore branding/start guide pass: renamed every player-facing game title and accessibility label to `BCD Encore Royale`, replaced the `Celestefall / Ash Web Port` HUD lockup with `BCD Encore Royale / Live Battle`, and rewrote the opening help card as a neutral `How to Play` guide covering movement/aim, jump and wall-jump, dash, held aiming/release shooting, sword direction, and automatic wall cling. Legacy repository/license references and the internal test hook retain their upstream Celestefall names because they are not presented as the app title.
- Encore branding QA: the deterministic canvas harness kept the live arena state clean, while full-viewport browser inspection confirmed the `How to Play` card appears immediately on load and all BCD branding remains readable at 390×844 portrait and 844×390 landscape. Narrow-screen branding was split into compact lines to avoid the character carousel. Page title and accessibility snapshots report `BCD Encore Royale`; browser console reported zero errors or warnings.

## 2026-09-03 — Full-body chroma, authored slug, and frame-pacing pass

- Extended Ash's runtime chroma key from the head/torso subset across the full costume material, including mesh-backed chest, arms, pelvis, legs, and pack. Mesh attachments now point at the recolored texture, rotated atlas regions use their correct packed dimensions, and skin, hair, hands, weapons, slashes, and effects remain untouched.
- Ash's original bandana/head material is now the chroma reference for a 180-degree complementary accent, while the rest of the upper body follows the player's selected roster color. Repeated selections reuse cached recolors.
- Restored the original Bullet Age slug rig in gameplay. The former green oval is retained only as a load-failure fallback, and both independent slugs keep their authored walk/hit/death/respawn lifecycle.
- Replaced per-frame Spine triangle work on the main canvas with per-rig pose caches. Player, bot, and creature poses refresh at distance/role-appropriate rates and composite as single images; the main loop no longer duplicates renders on high-refresh displays.
- Baked fixed backgrounds, collision masonry, ledges, lighting veil, and note glyphs into reusable canvases; culled offscreen zones, bursts, projectiles, particles, actors, and creatures; removed hot-loop array/object churn; compacted transient arrays in place; and stopped allocating room payloads faster than their send cadence.
- Performance QA: a five-second Chromium run delivered 302 animation frames at 16.59 ms average, 16.70 ms p95, 16.80 ms worst, and zero frames over 20 ms. The same run reported zero browser errors. Color switches completed in 49–54 ms when a new texture was required and effectively instantly from cache.
- Gameplay QA: deterministic movement/jump/dash passed with both fighters and all four creature rigs loaded. Visual checks confirmed selected color across Ash's chest and arms, complementary bandana/head material, preserved face/hair/weapon colors, a complete unclipped upward sword arc, and the authored slug. A two-hit slug test verified wounded, death, two-second respawn, and restored full health.
- TODO: a future showcase pass can replace Spine Canvas2D's remaining triangle clipping with a batched WebGL2 renderer and shader-based chroma. This is no longer required for stable 60 Hz frame pacing, but would further reduce pose-refresh CPU cost and color-change latency on low-end mobile hardware.

## 2026-09-03 — Mobile and embedded performance measurement track

- Added `tests/mobile-performance.mjs`, a repeatable Chromium matrix covering 390×844 portrait and 844×390 landscape, standalone and real iframe embedding, 60/120-Hz target profiles, startup/decode timing, resources, long tasks, heap/CDP counters, and five-second frame pacing. `docs/iframe-harness.html` supplies a same-origin real iframe surface; `docs/PERFORMANCE.md` defines device/thermal/parity acceptance criteria.
- The benchmark injects a read-only seven-rig load counter before navigation and waits until every authored rig has finished loading, separating startup/decode from steady-state frame pacing without shipping an instrumentation global.
- Shared immutable Spine source assets (`.json`, `.atlas`, decoded `.png`) across rigs while retaining a separate atlas/SkeletonData per character, which preserves chroma and attachment isolation. Ash, one shared Player2 source for selectable/bot rigs, Bat, and Slug now report four source-cache entries instead of decoding Player2’s 2048×2048 (16 MiB decoded) atlas for each rig.
- Local Chromium evidence: readiness-gated 60-Hz portrait/landscape runs completed in 621–729 ms standalone/iframe startup with zero browser errors; 5-second p95 was 16.7–16.8 ms. The target-120 profiles correctly report the host’s observed ~59–60 Hz cadence, so they are regression coverage only and cannot certify a physical 120-Hz display. The full three-screen route ended with live Ash, Player2, bat, and slug rigs and zero browser errors; visual screenshot inspection passed.
- TODO: record three consecutive runs on representative 60-Hz and 120-Hz Android/iOS phones (including Safari) to validate thermal behavior, real refresh cadence, and post-warmup heap settling before release.

## 2026-09-05 — Player Two idle blink head correction

- Reproduced the idle pop with the shipped Spine runtime: the Heads slot switches to ash_head_17 at 0.5s and back to ash_head_02 at 0.6667s. Player2's blink attachment was 246x230 versus the normal head's 226x219, with a different center/rotation. Ash's corresponding attachments already have matching dimensions. HeadFacing scale stayed constant and its position remained continuous; there is no erroneous head-scale keyframe or blend spike to remove.
- Corrected only the Player2 blink attachment's width, height, x/y, and rotation to match its open-eye attachment. Preserved the blink texture, slot keys, all bone curves, idle breathing, and the runtime's 90ms blend. A semantic comparison against HEAD confirmed every other rig value is unchanged.
- Added tests/idle-head.mjs: headless Chromium loads both real rigs, samples 1,200 frames (ten idle loops at 120 Hz), captures before/during/after-blink frames, and exercises ten action-to-idle transitions per rig. Assertions require both blink images, moving idle pose, continuous head position, stable head dimensions, finite transition poses, and no page errors. The original attachment showed about 1.72 and 0.946 game-pixel dimension jumps; corrected head dimensions vary only by floating-point noise (under 0.00003 pixels including settled transitions).
- Verification passed: node tests/idle-head.mjs http://127.0.0.1:4193/ output/idle-head-after; standard develop-web-game client with tests/three-screen.actions.json. Inspected the rig contact sheet and gameplay screenshot; no browser errors. Evidence in output/idle-head-before, output/idle-head-after, output/idle-head-gameplay (ignored local artifacts).
- Local fix only; not committed, pushed, or published. The separate Celestefall-renderer-wgl2 checkout is untouched and must receive this asset correction when its renderer work is integrated. No further local TODO for this defect.
- Publication follow-up: user authorized live release. Confirmed FPS task completed/inactive and GitHub Pages serves main/docs. Committed only Player2.json and tests/idle-head.mjs as 6b0bfe6, pushed main, and confirmed GitHub Pages built that exact commit. Fetched the public JSON and verified corrected blink metadata. Headless tests/idle-head.mjs against https://ijustcreate.github.io/Celestefall/ passed all idle/transition assertions with zero page errors; live contact sheet inspected. Renderer/performance checkout untouched. Local progress.md remains uncommitted.

## 2026-09-05 — Shared heart drop helper
- Added server/heart-drops.mjs: 50% bat/slug drop threshold, server-only falling pickups, +1 capped health, one collector, full/dead/disconnected skip, 30s expiry.
- Added drawHearts renderer (reapplied after concurrent file restoration). No player death drops or local random rolls.
- Three helper tests pass and game.js syntax passes. Headless smoke captured output/hearts-smoke; combined two-client/late-join validation and publishing owned by server synchronization task.

## 2026-09-05 — Capture markers rest on crown platforms

- Added CAPTURE_MARKER_Y from authored crown ledge geometry; fill task integrated it in drawCaptureZones for all visual elements. Marker tops are 118/82/118 and bottoms 180/144/180 (50px higher). Capture zone geometry and behavior are unchanged.
- Headless output/check-markers.mjs passed exact canvas outline placement for all three markers, unchanged gameplay Y assertions, and zero page errors. Inspected all three screenshots and standard web-game client gameplay screenshot/state (output/marker-gameplay).
- Coordinated shared draw function with fill task and publication with server/reload tasks; no independent commit or push. Ready for coordinated release.
`n- 2026-09-05 health-dot visual: removed only the local player shared gray 26x7 backing in drawPlayer. All three floating dots still render at full health; partial health retains gray empty dots. Headless standard client and instrumented full (3/3) / partial (2/3) canvas checks passed, screenshots inspected, zero browser errors. Artifacts: output/health-dots-qa; no independent commit/push.

## 2026-09-05 — BCD account display-name handoff
- Launcher sends normalized currentUser().name on iframe load and ready, with exact target origin, and refreshes changed account display metadata while open. No credentials passed.
- Game refreshes existing room profile through track() on repeat identity messages; no competing connection or authority changes.
- tests/account-name.mjs passed with real launcher + iframe: Felix, another account/rename, guest fallback, rejected non-parent sender, zero page errors. Standard headless movement smoke passed; screenshot inspected.
- v1.1 coordinated release owner handles publishing and live two-client authoritative snapshot validation; this task made no pushes.


## 2026-09-05 — Capture marker color fill
- Marker interior now fills bottom-to-top in each capturing team's color, proportional to authoritative progress. Existing owner color stays on the border and owner names remain until completion; recapture percentage is visible for different players and same-user color changes. Multiple colors retain separate progress lanes during contest, labeled CONTESTED.
- Reads server snapshot captureProgressByColor / captureContested supplied by authority task, with offline local-progress fallback. Text-state output exposes per-color percentages and contest state. Integrated placement task's CAPTURE_MARKER_Y for every marker visual coordinate; gameplay bounds unchanged.
- Headless tests/capture-marker.mjs passed fill heights at 25/50/75%, fixed bottom, remote color, same-ID changed color, owner retention, contest lanes, and completed recapture. Scenario screenshot inspected. Standard develop-web-game three-screen client passed with no page-error artifact; gameplay screenshot/state inspected. Syntax and git diff --check passed.
- No independent commit/push: release task owns combined version/publishing. Authority task owns end-to-end server capture tests. Recovered first edit's Windows encoding failure from committed f0404d4 and coordinated reapplication of concurrent heart rendering; further edits used narrow apply_patch.

## 2026-09-05 — Bat corpse landing
- Shared deterministic docs/corpse-physics.js + server/corpse-physics.mjs: dead bats fall under gravity, swept first-surface landing prevents tunneling, settled bodies follow moving supports. Bat lifetime starts counting down on landing; player death untouched.
- Offline death branches and server authority use same helper; dead bats simulate offscreen. Renderer preserves death pose, aligning visible bounds with landing feet.
- Validation: 6 node corpse regressions including actual server snapshot/lifetime pass; tests/bat-corpse-headless.mjs verifies offscreen fall, ledge landing, death animation, respawn and zero page errors. Screenshots inspected in output/bat-corpse. Skill headless movement smoke passes.
- Publishing delegated to server-sync task for combined v1.1; no independent push.

- Publication completed: game commit 0661fa5 built at https://ijustcreate.github.io/bcd-kc-encore/ and the compatible old /Celestefall/ path; BCD launcher commit a0511be built live. Headless public BCD iframe check passed canonical URL, displayed v1.1, fresh Reload, close cleanup, OFFLINE PRACTICE and zero page errors; embedded screenshot inspected (output/live-release-1.1).
- Remaining: public authoritative hosting choice/account required; no paid resources created. Dedicated server is locally verified but not publicly hosted. Physical phone FPS remains for diagnostics task. User reports missing fill during actual offline red-to-green recapture; capture task reopened to reproduce. Diagnostics and capture tasks coordinate next 1.2; they own current new shared-file edits. Preserve separate unpublished WebGL2 work and unrelated BCD prototype/polling changes.
